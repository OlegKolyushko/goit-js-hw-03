const user = {
    name: 'Mango',
    age: 20,
    hobby: 'html',
    premium: true,
  };
// const countProps = function(obj) {
//     const totalValues = Object.values(user)
//     console.log(totalValues.length); 
//   };
//   console.log(countProps(user));
  const totalValues = Object.values(user)
    console.log(totalValues.length); 